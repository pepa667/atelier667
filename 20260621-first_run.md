# CONTEXT PROMPT: ATELIER667 (GROUND-ZERO RECOVERY & SYNC)

## 1. IDENTITY & CONCEPT
* **Project Name:** Atelier667
* **Philosophy:** "Artesanato Técnico" — Fusion of visual art, digital software (React/Astro), custom microcontrollers (ESP32/C++), and heavy physical fabrication (woodworking, 3D printing).
* **The Name:** "Atelier" (French craftsman's workshop) + "667" (The Next Level / Level UP [+1] over raw chaos).
* **Tone:** Technical, gritty, informal, zero-noise conversational overhead.

## 2. MONOREPO ARCHITECTURE (LOCAL OK / GITHUB SYNCED)
Root layout deployed at `~/Desktop/atelier667/` and fully committed to remote GitHub repository (`main` branch).
* `sanity-studio/` — Headless CMS (Content Lake on Sanity.io cloud) using JavaScript and updated CLI v3 syntaxes.
* `astro-front/` — Web UI running Astro 5.0 + Tailwind CSS v4 (Vite plugin architecture) + React + `@sanity/client`.

## 3. COMPONENT & DATA SCHEMAS (DEPLOYED)
* **`projeto` (Projetos Maker):** Fields for localized titles, slug, status tracker (concluido, desenvolvimento, pausado), 0-100% progress metric, maker categories (marcenaria, 3d, eletronica, programacao), tags array, mainImage, and markdown-like logs.
* **`drop` (Microblog / Drops):** Fields for immediate 280-char bench updates and auto-timestamping.

## 4. CURRENT STATE & VERIFIED MILESTONES
* **Sanity Data Connection:** Verified. Astro is successfully executing GROQ queries against the cloud lake. A test entry (`BeBop Core`, 60%, #ESP32 #Firmware) was successfully parsed and injected into the frontend pipeline.
* **Tailwind v4 Integration:** Resolved. Switched implementation from legacy Astro integrations to the new Vite compilation layer via `src/styles/global.css` using `@import "tailwindcss";`. Layout rendering dark mode (`bg-zinc-950`) flawlessly.
* **Git Versioning:** Global `.gitignore` injected at root. All core configurations, stylesheets, and index modules pushed to origin.

## 5. NEXT SPRINT STEPS
* **Interface Enrichment:** Build out frontend data-fetching logic for the `drop` schema to support the live microblog section.
* **Production Pipeline:** Configure cloud environment injection (`.env`) for the Sanity Project ID and set up automatic deployment webhooks (Vercel/Netlify) triggered on database `Publish`.